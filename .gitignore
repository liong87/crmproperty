node_modules
.next
.open-next
.env
.env*.local
*.log
.DS_Store
.wrangler
coverage
next-env.d.ts

# Temp files
_tmp_*
_backup_before_patch
_to_delete
# Transfer artefacts and local scratch - never commit
*.zip
*.backup.gz
*.dump
*.dump.gpg
_apply*.zip
_backup_before_patch/
_to_delete/
*.tsbuildinfo
tsconfig.tsbuildinfo
.env.local.*

# Screen recordings kept for reference - too large for git
samples/*.mp4
samples/_frames/

# Source documents from the previous agency - reference only, not app inputs
references/
